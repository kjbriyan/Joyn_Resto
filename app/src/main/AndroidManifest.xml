<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.joyn.tenant">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".sharedpref.PrefsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.fragment.promo.activity.BuatPromoActivity"
            android:theme="@style/Login"/>
        <activity android:name=".activity.fragment.promo.activity.PilihMenuActivity"
            android:theme="@style/Login">

        </activity>
        <activity android:name=".activity.DetailOrderActivity"
            android:parentActivityName=".activity.NavActivity"
            android:label="Detail Order">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.NavActivity" />
        </activity>
        <activity android:name=".activity.OtpActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar"/>
        <activity
            android:name=".activity.NavActivity"
            android:theme="@style/Login" />
        <activity
            android:name=".activity.RegistrasiActivity"
            android:theme="@style/Login" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>